This script turns text into dramatic prose. 
This is literally my first few lines of code. 

How it works?
It takes an Input, splits it, and then, iterates it over and adds '...' to join them together. 
In the end, I strip the additional (redundant) using the strip() function. 
